import { RoundedBlockDirective } from './rounded-block.directive';

describe('RoundedBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundedBlockDirective();
    expect(directive).toBeTruthy();
  });
});
